
# SimmC_Auto 软件使用帮助
 - 作者: 3072252442（帕秋莉·阿希欧姆🌙）
 - 日期: 2026/1/17 12:37

## 1. 配置
 - 首先运行SimmC_Auto.exe，会生成两个文件`patten.json`和`config.json`，第一个不用管，点开`config.json`
 - 然后你会看到一些配置键值对，找`MINECRAFT`区域，把`USER_ID`换成自己的玩家ID
 - 去找`MinecraftLogListener`，看`log_path`
 - 换成自己的`latest.log路径`（去你客户端的logs文件夹下找，会有）
 - 再次启动SimmC_Auto.exe，完事
 - 如果出现`WARNING`则不用管，你可以设置 `mode` 为 `file` 模式

## 2. 高级
 - 如果你想程序运行的更快，建议把 `mode` 设置为 `socket` 模式，但是要额外配置，下面是方法
 - 你可以看到我提供的`mc-agent.jar`在文件夹，把路径记好
 - 退出游戏，然后点开PCL->版本设置->设置->高级->Java虚拟机参数
 - 把下列字符串复制进去，记得要替换成自己的mc-agent.jar路径
 ```bash
 -javaagent:"你的mc-agent.jar路径"
 ```
 - 再次启动游戏，随后启动程序，完事